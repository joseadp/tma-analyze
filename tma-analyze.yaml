apiVersion: apps/v1
kind: Deployment

metadata:
  name: tma-analyze

spec:
  selector:
    matchLabels:
      run: tma-analyze
  template:
    metadata:
      labels:
        run: tma-analyze
    spec:
      containers:
      - name: tma-analyze
        image: tma-analyze:0.1
